import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerText: {
    marginLeft: "-35%",
    fontWeight: "bold",
  },
  headerIcon: {
    fontSize: 24,
    color: "white",
  },
});

export default styles;
